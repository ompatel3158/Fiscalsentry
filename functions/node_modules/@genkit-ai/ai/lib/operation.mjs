const OPERATION_CONFIG_CONTEXT_KEY = "config";
function toRunOptions(options) {
  if (!options) {
    return void 0;
  }
  const { config, context, ...rest } = options;
  if (config === void 0) {
    return { ...rest, context };
  }
  return {
    ...rest,
    context: {
      ...context,
      [OPERATION_CONFIG_CONTEXT_KEY]: config
    }
  };
}
export {
  OPERATION_CONFIG_CONTEXT_KEY,
  toRunOptions
};
//# sourceMappingURL=operation.mjs.map