import {
  FirestoreSessionStore as GcpFirestoreSessionStore
} from "@genkit-ai/google-cloud/beta";
import { getFirestore } from "firebase-admin/firestore";
class FirestoreSessionStore extends GcpFirestoreSessionStore {
  constructor(opts) {
    super({
      ...opts,
      db: opts?.db ?? (opts?.firebaseApp ? getFirestore(opts.firebaseApp) : getFirestore())
    });
  }
}
export {
  FirestoreSessionStore
};
//# sourceMappingURL=firestore.mjs.map